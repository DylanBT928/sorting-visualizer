#include <vector>

class bubble_sort
{
public:
    void sort(int i, std::vector<int> &values)
    {
        bool swapped = false;
        for (int j = 0; j < values.size() - i - 1; j++)
        {

        }
    }
};
